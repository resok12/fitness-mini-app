# 🚀 Инструкция по развертыванию Fitness Mini App

## 📋 Что мы создали

1. **Backend (Node.js + Express + MongoDB)** - API для хранения данных
2. **Frontend (HTML/CSS/JS)** - Telegram Mini App
3. **Telegram Bot** - Для запуска приложения
4. **API Client** - Для связи frontend с backend

---

## 🛠️ Шаг 1: Установка зависимостей

### Установите Node.js и MongoDB

**Node.js:**
```bash
# Скачайте с https://nodejs.org/
# Или через nvm:
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 20
```

**MongoDB:**
```bash
# macOS (через Homebrew):
brew tap mongodb/brew
brew install mongodb-community

# Ubuntu/Debian:
sudo apt-get install mongodb

# Или используйте MongoDB Atlas (облачная версия):
# https://www.mongodb.com/cloud/atlas
```

### Установите пакеты проекта

```bash
npm install
```

Это установит:
- express (веб-сервер)
- mongoose (MongoDB ORM)
- cors (для кросс-доменных запросов)
- multer (загрузка файлов)
- dotenv (переменные окружения)
- node-telegram-bot-api (Telegram бот)

---

## 🤖 Шаг 2: Создание Telegram бота

### 2.1 Создайте бота у @BotFather

1. Откройте Telegram
2. Найдите **@BotFather**
3. Отправьте команду `/newbot`
4. Введите имя бота (например: **Fitness Pro**)
5. Введите username бота (например: **fitness_pro_bot**)
6. **Сохраните токен!** (выглядит так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2.2 Настройте Menu Button

Отправьте @BotFather эти команды:

```
/setmenubutton
@ваш_бот
Fitness App
https://your-domain.com
```

### 2.3 Добавьте описание бота

```
/setdescription
@ваш_бот
Твой персональный фитнес-помощник! Отслеживай прогресс, питание и тренировки. 💪
```

### 2.4 Добавьте команды

```
/setcommands
@ваш_бот

start - Открыть приложение
help - Помощь
stats - Статистика прогресса
today - Тренировка дня
```

---

## 🔐 Шаг 3: Настройка окружения

### Откройте файл `.env` и заполните:

```env
# Порт сервера
PORT=3000

# MongoDB (локально или MongoDB Atlas)
MONGODB_URI=mongodb://localhost:27017/fitness-app
# Или для Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/fitness-app

# Токен бота от BotFather
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# URL вашего приложения (после деплоя)
WEB_APP_URL=https://your-domain.com

# Настройки
MAX_FILE_SIZE=52428800
```

---

## 🚀 Шаг 4: Локальное тестирование

### 4.1 Запустите MongoDB

```bash
# macOS/Linux:
mongod

# Или если установлен как сервис:
brew services start mongodb-community
# или
sudo systemctl start mongodb
```

### 4.2 Запустите backend

```bash
node server.js
```

Вы должны увидеть:
```
✅ Connected to MongoDB
🚀 Server running on port 3000
📱 API: http://localhost:3000/api
```

### 4.3 Запустите бота (в отдельном терминале)

```bash
node bot.js
```

Вы должны увидеть:
```
🤖 Telegram Bot запущен!
```

### 4.4 Локальное тестирование с ngrok

Для тестирования Mini App локально используйте **ngrok**:

```bash
# Установите ngrok: https://ngrok.com/
ngrok http 3000
```

Вы получите URL типа: `https://abc123.ngrok.io`

**Важно:** Обновите `WEB_APP_URL` в `.env`:
```env
WEB_APP_URL=https://abc123.ngrok.io/fitness-mini-app.html
```

Перезапустите бота после изменения `.env`.

---

## 🌐 Шаг 5: Деплой в продакшен

### Вариант 1: Heroku (простой)

1. **Создайте аккаунт на Heroku**: https://heroku.com

2. **Установите Heroku CLI**:
```bash
npm install -g heroku
heroku login
```

3. **Создайте приложение**:
```bash
heroku create fitness-mini-app
```

4. **Добавьте MongoDB Atlas** (бесплатный план):
   - Зарегистрируйтесь на https://www.mongodb.com/cloud/atlas
   - Создайте кластер
   - Получите connection string
   - Добавьте в Heroku:
```bash
heroku config:set MONGODB_URI="mongodb+srv://..."
heroku config:set TELEGRAM_BOT_TOKEN="ваш_токен"
```

5. **Деплой**:
```bash
git init
git add .
git commit -m "Initial commit"
heroku git:remote -a fitness-mini-app
git push heroku main
```

6. **Обновите URL бота**:
```bash
heroku config:set WEB_APP_URL="https://fitness-mini-app.herokuapp.com/fitness-mini-app.html"
```

### Вариант 2: Vercel + MongoDB Atlas

1. **Frontend на Vercel**:
```bash
npm install -g vercel
vercel login
vercel
```

2. **Backend на Railway**:
   - Зарегистрируйтесь на https://railway.app
   - Подключите GitHub репозиторий
   - Добавьте переменные окружения
   - Railway автоматически задеплоит

### Вариант 3: VPS (DigitalOcean, AWS, и т.д.)

1. **Создайте сервер**
2. **Установите Node.js, MongoDB, PM2**:
```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs mongodb
npm install -g pm2
```

3. **Склонируйте проект и установите**:
```bash
git clone your-repo
cd your-repo
npm install
```

4. **Запустите с PM2**:
```bash
pm2 start server.js --name fitness-api
pm2 start bot.js --name fitness-bot
pm2 save
pm2 startup
```

5. **Настройте Nginx** (опционально):
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

6. **Получите SSL сертификат**:
```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

---

## 🧪 Шаг 6: Тестирование

### 6.1 Откройте бота в Telegram

Найдите своего бота по username и отправьте `/start`

### 6.2 Нажмите "Открыть приложение"

Должно открыться ваше Mini App!

### 6.3 Протестируйте функции:

✅ **Мои данные**: Добавьте замер
✅ **Питание**: Отметьте прием пищи, добавьте воду
✅ **Состояние**: Обновите состояние
✅ **Тренировки**: Откройте упражнение, запустите таймер
✅ **Чат**: Отправьте сообщение

### 6.4 Проверьте базу данных

```bash
# Подключитесь к MongoDB
mongosh

# Выберите базу
use fitness-app

# Посмотрите коллекции
show collections

# Посмотрите пользователей
db.users.find().pretty()

# Посмотрите замеры
db.measurements.find().pretty()
```

---

## 📱 Шаг 7: Обновление frontend для работы с API

Добавьте в начало `fitness-mini-app.html` (после тега `<script src="https://telegram.org/js/telegram-web-app.js">`):

```html
<script src="api.js"></script>
```

И обновите функции для работы с API (уже готово в обновленной версии).

---

## 🔧 Troubleshooting (Решение проблем)

### Проблема: Бот не отвечает
**Решение:**
- Проверьте токен в `.env`
- Убедитесь что `bot.js` запущен
- Проверьте логи: `tail -f bot.log`

### Проблема: Mini App не открывается
**Решение:**
- Убедитесь что `WEB_APP_URL` правильный
- Проверьте что сервер запущен
- Откройте URL в браузере напрямую

### Проблема: Ошибка подключения к MongoDB
**Решение:**
- Проверьте что MongoDB запущен: `mongosh`
- Проверьте `MONGODB_URI` в `.env`
- Для Atlas проверьте IP whitelist

### Проблема: Файлы не загружаются
**Решение:**
- Создайте папку `uploads/`: `mkdir uploads`
- Проверьте права: `chmod 755 uploads`
- Увеличьте `MAX_FILE_SIZE` если нужно

### Проблема: CORS ошибки
**Решение:**
- Убедитесь что CORS включен в `server.js`
- Проверьте что домены совпадают
- Для production добавьте правильные origins

---

## 📊 Мониторинг и логи

### Логи PM2
```bash
pm2 logs fitness-api
pm2 logs fitness-bot
```

### Перезапуск сервисов
```bash
pm2 restart fitness-api
pm2 restart fitness-bot
```

### Статус
```bash
pm2 status
pm2 monit
```

---

## 🎨 Дальнейшие улучшения

### Следующие шаги:
1. ✅ Добавить авторизацию тренеров
2. ✅ Создать админ-панель для тренеров
3. ✅ Добавить push-уведомления
4. ✅ Интегрировать платежи (Telegram Stars)
5. ✅ Добавить графики прогресса (Chart.js)
6. ✅ Сделать PWA версию
7. ✅ Добавить AI-рекомендации

### Полезные ресурсы:
- 📖 Telegram Mini Apps: https://core.telegram.org/bots/webapps
- 📖 Telegram Bot API: https://core.telegram.org/bots/api
- 📖 MongoDB Docs: https://docs.mongodb.com/
- 📖 Express.js: https://expressjs.com/

---

## 🆘 Нужна помощь?

Если что-то не работает:
1. Проверьте логи сервера и бота
2. Проверьте все переменные в `.env`
3. Убедитесь что все сервисы запущены
4. Проверьте версии Node.js и MongoDB

---

## 🎉 Готово!

Теперь у вас есть полноценное Telegram Mini App для фитнеса!

**Протестируйте:**
1. Откройте бота в Telegram
2. Нажмите `/start`
3. Откройте приложение
4. Попробуйте все функции

**Успехов! 💪🚀**
