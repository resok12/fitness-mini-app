# ⚡ Быстрый старт за 5 минут

## Вариант 1: Локально (для тестирования)

### 1. Установите зависимости
```bash
npm install
```

### 2. Запустите MongoDB
```bash
# Просто запустите Docker с MongoDB:
docker run -d -p 27017:27017 --name fitness-mongo mongo:7.0

# Или установите MongoDB локально:
# macOS: brew install mongodb-community
# Ubuntu: sudo apt install mongodb
```

### 3. Создайте бота в Telegram
1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Введите имя: `Fitness Pro Test`
4. Введите username: `fitness_pro_test_bot`
5. **Скопируйте токен** (например: `6123456789:ABCdefGHIjklMNO...`)

### 4. Создайте файл .env
```bash
PORT=3000
MONGODB_URI=mongodb://localhost:27017/fitness-app
TELEGRAM_BOT_TOKEN=ваш_токен_здесь
WEB_APP_URL=https://your-ngrok-url.ngrok.io/fitness-mini-app.html
```

### 5. Запустите ngrok (для локального теста)
```bash
# Скачайте: https://ngrok.com/download
ngrok http 3000
```
Скопируйте URL (например: `https://abc123.ngrok.io`)
и обновите `WEB_APP_URL` в `.env`

### 6. Запустите приложение
```bash
# В одном терминале:
node server.js

# В другом терминале:
node bot.js
```

### 7. Готово! 🎉
Откройте бота в Telegram и нажмите `/start`

---

## Вариант 2: Docker (самый простой)

### 1. Создайте файл .env
```bash
TELEGRAM_BOT_TOKEN=ваш_токен
WEB_APP_URL=https://your-domain.com/fitness-mini-app.html
```

### 2. Запустите
```bash
docker-compose up -d
```

### 3. Готово! ✅
Приложение запущено на `http://localhost:3000`

---

## Вариант 3: Heroku (бесплатный хостинг)

### 1. Установите Heroku CLI
```bash
npm install -g heroku
heroku login
```

### 2. Создайте приложение
```bash
heroku create my-fitness-app
```

### 3. Добавьте MongoDB Atlas
- Зарегистрируйтесь на https://www.mongodb.com/cloud/atlas
- Создайте бесплатный кластер
- Получите connection string

### 4. Настройте переменные
```bash
heroku config:set MONGODB_URI="mongodb+srv://..."
heroku config:set TELEGRAM_BOT_TOKEN="ваш_токен"
heroku config:set WEB_APP_URL="https://my-fitness-app.herokuapp.com/fitness-mini-app.html"
```

### 5. Деплой
```bash
git init
git add .
git commit -m "Initial commit"
git push heroku main
```

### 6. Готово! 🚀
Ваше приложение на `https://my-fitness-app.herokuapp.com`

---

## Проверка работы

### ✅ Checklist:
- [ ] MongoDB запущен
- [ ] Server.js работает (видите "Server running on port 3000")
- [ ] Bot.js работает (видите "Telegram Bot запущен")
- [ ] Бот отвечает на /start в Telegram
- [ ] Mini App открывается по кнопке
- [ ] Можно добавить замер
- [ ] Можно отметить прием пищи
- [ ] Таймер работает

### 🐛 Если что-то не работает:

**Бот не отвечает:**
```bash
# Проверьте токен
echo $TELEGRAM_BOT_TOKEN

# Проверьте что bot.js запущен
ps aux | grep bot.js
```

**Mini App не открывается:**
```bash
# Проверьте URL
curl http://localhost:3000/fitness-mini-app.html

# Проверьте что WEB_APP_URL правильный
cat .env | grep WEB_APP_URL
```

**Ошибка MongoDB:**
```bash
# Проверьте что MongoDB запущен
docker ps | grep mongo
# или
mongosh
```

---

## 📱 Тестирование в Telegram

1. Найдите своего бота в Telegram
2. Отправьте `/start`
3. Нажмите "🚀 Открыть приложение"
4. Протестируйте все 5 разделов:
   - Мои данные → Добавить замер ✅
   - Питание → Отметить еду, добавить воду ✅
   - Состояние → Обновить состояние ✅
   - Тренировки → Открыть упражнение ✅
   - Чат → Отправить сообщение ✅

---

## 🎨 Кастомизация

### Изменить цвета:
Откройте `fitness-mini-app.html` и измените CSS переменные:
```css
:root {
    --accent: #1A5F3F;  /* Ваш цвет */
}
```

### Добавить свои упражнения:
Отредактируйте раздел "Тренировки" в HTML

### Изменить бота:
Отредактируйте `bot.js` для новых команд

---

## 💡 Полезные команды

```bash
# Посмотреть логи
tail -f logs/server.log
tail -f logs/bot.log

# Перезапустить сервисы
npm run restart

# Проверить статус
curl http://localhost:3000/api/health

# Посмотреть базу данных
mongosh fitness-app
> db.users.find()

# Очистить базу данных
mongosh fitness-app
> db.dropDatabase()
```

---

## 🆘 Помощь

Не работает? Проверьте:
1. ✅ Node.js установлен (`node -v`)
2. ✅ MongoDB запущен
3. ✅ .env файл создан
4. ✅ Токен бота правильный
5. ✅ WEB_APP_URL доступен

**Всё ещё не работает?**
Проверьте [DEPLOYMENT.md](DEPLOYMENT.md) для подробной инструкции.

---

**Готово! Удачи! 🚀💪**
